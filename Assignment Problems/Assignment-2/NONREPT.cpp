#include <iostream>
using namespace std;
#define rep(j, size) for (int j = 0; j < size; j++)
int main()
{
        int t;
        cin >> t;
        while (t--)
        {
                int n;
                cin >> n;
                int arr[n];
                rep(i, n) cin >> arr[i];
        }

        return 0;
}